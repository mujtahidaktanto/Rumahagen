# RumahAgen SaaS — Repository Gabungan (Spesifikasi + Wireframe + Kode)

Repo ini menggabungkan dua sumber upload asli menjadi satu struktur GitHub, dan kini
sudah mulai berisi **kode implementasi nyata** untuk menutup residual yang ditemukan
saat deep scan (lihat `audit/WIREFRAME_DEEP_SCAN_REPORT.md` dan
`CHECKLIST_RESIDUAL_IMPLEMENTASI.md`).

1. **`Core_baru_RumahAgen-SaaS-GitHub-Ready.zip`** → spesifikasi/governance lengkap
   (STEP 00–14 + P1–P16).
2. **`WF_Wire.zip`** → 27 paket wireframe (WF-00 s.d. WF-11), direorganisasi ke satu
   skema folder konsisten.

## Status implementasi (per modul)

| Area | Status | Keterangan |
|---|---|---|
| **M10 — Authorization (RBAC/Permission/RLS)** | ✅ **Kode nyata** | `supabase/migrations/0001`–`0010`, generate otomatis dari `STEP12-01_ROLE_PERMISSION_MASTER_MATRIX.csv` (90 permission, 345 grant) — menutup residual D13-06, D13-15, R-02, D13-04 |
| **Konvensi API global** | ✅ **Kode nyata** | `apps/web/lib/api/*` — menutup D13-16 s.d. D13-23 |
| **Scaffold Next.js** | ✅ **Kode nyata** | `apps/web/` — `package.json`, Supabase client, 1 contoh route nyata (`GET /api/authorization/roles`) yang sudah query ke migration M10 |
| **M09 — Admin Console** | ✅ **Kode nyata + REST API** | `supabase/migrations/0011`–`0014` — `system_configs`, `audit_logs`, `notification_templates` (baru), `public_announcement_promotion` (baru) — menutup residual R-06, D13-05, D13-08, D13-13; D13-12 tertutup di level permission, D13-14 CLOSED (lihat M13). **STEP11-A (API-129/149/238/239) + M11 (API-135/136)**: `apps/web/app/api/admin/config/**`, `admin/audit-logs`, `admin/notification-templates/**`, `banners`/`admin/banners/**` — diuji end-to-end sebagai Superadmin nyata. `admin/reports/export` (API-138) dan `admin/internal-users` (API-139-142) belum ada — dataset/skemanya tidak evidenced |
| **M13 — Provider Catalogue / BYOK** | ✅ **Kode nyata + REST API** | `supabase/migrations/0015`–`0016` — `ai_providers`, `agent_ai_connections` (status 5-state + trigger lifecycle), fungsi `admin_force_provider_connection()` — menutup R-07, D13-11 penuh, D13-09/D13-10 penuh di lapisan HTTP. **STEP11-B10 + kelengkapan penuh**: `apps/web/app/api/ai-providers/**`, `ai-connections/**`, `admin/ai-connections/**` + `lib/crypto/byok.ts` (enkripsi AES-256-GCM baru, sebelumnya belum ada pola enkripsi di repo) — diuji end-to-end menyeluruh (create/rotate/test/force-disable/reverse/soft-disconnect, termasuk verifikasi `encrypted_api_key` benar-benar ter-enkripsi di DB). **`0080` (ADD-NEW)**: 2 kolom generik `public_identifier` (plaintext, non-rahasia) + `encrypted_secondary_key` (terenkripsi persis seperti `encrypted_api_key`) — ditemukan lewat evaluasi nyata provider Cloudinary yang butuh 3 kredensial sekaligus (`cloud_name`/`api_key`/`api_secret`), skema lama cuma punya 1 field. Provider single-key (Gemini, dst.) membiarkan keduanya NULL, tidak ada perubahan perilaku. Diuji nyata: buat koneksi single-key DAN multi-kredensial berdampingan, rotasi `secondary_key`, verifikasi dekripsi ganda via `/test`, dan konfirmasi `encrypted_secondary_key` benar-benar terenkripsi di DB (bukan plaintext). `POST /ai-assistant/chat` (invokasi AI sungguhan) dan route upload/edit foto Cloudinary belum ada — adapter provider nyata menyusul batch terpisah |
| **M03 — Listing** + **M14 — Refresh Allowance/Entitlement** | ✅ **Kode nyata + REST API** | `supabase/migrations/0017`–`0020` — `listings` (skema penuh + trigger lifecycle), rantai kuota M14 (5 tabel), fungsi `refresh_listing()` memanggil `consume_refresh_allowance()` — menutup R-04, D13-01 penuh. **STEP11-B2 (API-025/026/027/028/029/035/237)**: `apps/web/app/api/listings/**` + `apps/web/app/api/agents/me/listings/**` — route REST nyata kedua di repo ini (setelah M10), diuji langsung ke project Supabase. Media/price-history/from-project/admin-queue (API-030–034/036–038) belum diimplementasi (tabelnya belum ada) |
| **M04 — Learning Session/Evidence/LP Economy/Partnership** + **M15 — Qualification/Award** | ✅ **Kode nyata + REST API penuh** | `supabase/migrations/0021`–`0027` — 8 tabel Session/Evidence M04, LP economy, Partnership Learning Result (baru), 5 tabel Qualification/Award M15, fungsi `grant_learning_points_from_purchase()` + `capture_qualification_evidence_from_session()`/`evaluate_qualification()` — menutup R-08, D13-02, D13-03 (lihat catatan gap terbuka: mesin awarding path/rule belum dibangun). **STEP11-B5 (API-086-115)**: `apps/web/app/api/learning/**` + `integrations/learning-session/**` — 21 route file, diuji end-to-end menyeluruh. Migration `0042` menutup gap RLS DELETE; `0043`+`0044`+`0045` memperbaiki **3 bug fungsional RLS/trigger** (scope 'own' Instructor tidak pernah terpenuhi di attendance/completion). **STEP11-B4 LP Economy + Partnership Learning Result (Gate PRE-00-F §51)**: `apps/web/app/api/agents/me/learning-points/**`, `admin/learning-point-adjustments`, `partnership-learning-results/**` — 7 route file, diuji end-to-end menyeluruh. Migration `0046` menutup gap RLS INSERT LP (fungsi `adjust_learning_points()`); fix mapping error 409 baru (Postgres `23514`) di `lib/api/handler.ts` berlaku untuk semua endpoint mutasi. **STEP11-B8 M15 Qualification/Evidence/Awarding (API-200-236)**: `apps/web/app/api/titles/**`, `title-authority-scopes/**`, `qualification-evaluations/**`, `qualification-evidence/**`, `awards/**` — 20 route file, saat dibangun hanya di atas 5 dari 14 tabel M15 (9 tabel "mesin konfigurasi jalur/aturan" belum ada migration-nya); diuji end-to-end menyeluruh termasuk pipeline M04→M15 evidence→evaluation→award penuh untuk pertama kalinya. M04 Learning Catalog/Activity (Fase 2) dan M15 Awarding Engine 9 tabel (Fase 3, lihat baris "100% tabel" di bawah) kini SUDAH punya migration+RLS lengkap TAPI belum punya REST API — menyusul batch terpisah |
| **M02 — Profile** | ✅ **Kode nyata + REST API** | `supabase/migrations/0029`–`0030` — `agent_profiles`, `agent_reviews` (auto-approve sesuai Gate PRE-00-D, bukan default literal STEP10-D) — di luar 31 residual asli, dikerjakan atas permintaan eksplisit. **STEP11-B1 (API-011/012/014/016/017/018/019/020)**: `apps/web/app/api/users/me`, `users/profile`, `agents/[id]`, `agents/[id]/credentials`, `agents/[id]/reviews`, `admin/agent-reviews/**` — 7 route file, diuji end-to-end (upsert profil, toggle public/private, review self & buyer, moderasi post-publikasi). `agent_verification_documents` (M01, migration 0049) HANYA punya `POST /users/verification-documents` (API-013 intake) — STEP11-B1 F11-B1-002/003 eksplisit melarang GET/PUT/DELETE, sengaja tidak dibangun |
| **M05 — Event** | ✅ **Kode nyata + REST API** | `supabase/migrations/0031`–`0032` — `events`, `event_provider_bindings`, `event_registrations` (termasuk Guest Registration) — di luar 31 residual asli, dikerjakan atas permintaan eksplisit. **STEP11-A (API-079-084)**: `apps/web/app/api/events/**` — diuji end-to-end penuh (create→publish→RSVP self/guest→delete). Migration `0039` menutup gap RLS DELETE yang hilang di `0031`; perbaikan mapping error 403 di `lib/api/handler.ts` berlaku untuk semua endpoint mutasi |
| **M06 — Developer/Project/Marketing Kit/Claim** | ✅ **Kode nyata + REST API penuh** | `supabase/migrations/0033`–`0035` — `developer_partners`, `developer_projects` (+ FK retroaktif ke `listings`/`events`), `marketing_kit`, `agent_project_claims` — menutup **R-05**. **STEP11-B3 (API-116-122) + kelengkapan penuh**: `apps/web/app/api/developer-projects/**`, `developer-partners/**`, `marketing-kit/**`, `project-media/**`, `claims/**`, `admin/developer-projects/**` — diuji end-to-end menyeluruh (self-service create, moderation-gate publish, media, marketing kit, klaim+review+approve, delete). Migration `0040` menutup gap RLS DELETE (pola sama seperti M05); migration `0041` memperbaiki **bug fungsional** RLS review-klaim yang ditemukan saat testing (Developer Partner tidak bisa approve klaim di project miliknya sendiri). Hanya Approval Claim PDF yang belum ada (tidak ada tabel/mekanisme fisik) |
| **M08 — Dashboard/Notification State** | ✅ **Kode nyata + REST API** | `supabase/migrations/0036` — `notifications` (+ dismiss/delivery-state) + fungsi `create_notification()` sebagai satu-satunya jalur insert — menutup **D13-07**. **STEP11-A (API-131/132/133/134/137)**: `apps/web/app/api/notifications/**`, `admin/notifications/push`, `dashboard/summary` — diuji end-to-end (push ditolak untuk Agent, diterima untuk Superadmin; read/dismiss/read-all/dashboard summary semua sesuai kontrak) |
| **M11 — Public Discovery/SEO** | ✅ **Kode nyata + REST API (evidence-scoped)** | `supabase/migrations/0037` + koreksi `0028` (permission `public_announcement_promotion` dari Tahap 2 dipindah ke M11 yang benar) — `static_public_content`. **Fase 1 "100% tabel"**: `0051_m11_url_redirects.sql` menambah `url_redirects` (SEO 301/302 redirect). **REST API ADD-NEW**: `apps/web/app/api/url-redirects/**` (GET `?old_path=` lookup + staff manage) — tidak ada literal endpoint di STEP11-B9, dibangun karena RLS publik tabel ini sendiri membuktikan maksud desainnya |
| **Fase 1 "100% tabel"** (hasil deep-scan `STEP10-D_ENTITY_TO_PHYSICAL_TABLE_RECONCILIATION.csv` — 94 entitas logis di spec, 54 tabel sudah ada sebelum fase ini) | ✅ **Kode nyata + REST API (evidence-scoped)** | `supabase/migrations/0047`–`0053` — 13 tabel PRESERVE/ADD-NEW spec: M03 (`listing_photos`/`videos`/`views`/`leads`/`price_history`/`amenities`/`listing_amenities`), `ref_villages`, M01 (`agent_verification_documents`), M12 (`organization_invitations`, `organization_document`), M11 (`url_redirects`), M07 (`dbr_simulations`). **2 bug ditemukan & ditutup saat migration**: `0054` (RLS listing child tables salah cek `status='active'`, seharusnya `'published'`) dan `0055` (Agent bisa self-approve `agent_verification_documents` — RLS scope 'own' tidak membedakan kolom). **REST API (Next.js + Zod), 20 route file** atas API-030-050 evidenced STEP11-B2 (M03 media/price-history/leads) + ADD-NEW (`listings/{id}/views`, `amenities`, `listing_amenities`, `ref-villages` — tabel yang RLS-nya sudah publik tapi tidak ada jalur REST sama sekali tanpa ini): `listings/[id]/media/**`, `price-history`, `views`, `cta-click`, `leads/**`, `agents/me/leads/**`, `admin/leads`, `amenities/**`, `ref-villages/**`. `organization_invitations` (M12, API-161-165 STEP11-B6): `organizations/[id]/invitations`/`join-requests`, `organization-invitations/[id]/accept`/`reject`, `agents/me/organization-invitations` — 5 route file. `dbr_simulations` (M07, ADD-NEW, tidak ada dokumen STEP11-B khusus M07): `dbr-simulations/**` — kalkulasi anuitas server-side pakai `dbr_config` (0008, threshold 35%/rate 8.5% sudah ada di DB) + pita "perlu_review" 10 percentage-point yang dievidence PRE-00-I. **`organization_document` (M12) SENGAJA TIDAK dibangun** — STEP11-B6 eksplisit: "No dedicated current document route... not grounds for inventing tables". **3 bug ditemukan & diperbaiki di kode REST** (bukan migration): `listing_views`/`listing_leads` INSERT gagal untuk anonim karena `.select()` kena RLS SELECT yang lebih ketat (gotcha arsitektural yang sama dari testing migration Fase 1, diperbaiki hapus `.select()`), dan lookup `dbr_config` gagal untuk Agent biasa karena RLS scope 'own' tidak cocok untuk config singleton (diperbaiki pakai admin client khusus lookup ini). Diuji end-to-end dengan 4 user throwaway (Superadmin/Agent×2/Buyer) — trigger anti self-approval `agent_verification_documents`/`organization_invitations` dikonfirmasi tetap terblokir lewat REST. Lihat `supabase/migrations/README.md` dan `apps/web/README.md` untuk rincian penuh |
| **Fase 2 "100% tabel"** — M04 Learning Catalog/Activity/Assessment | ✅ **Kode nyata + REST API penuh** | `supabase/migrations/0056`–`0063` — 13 tabel domain self-paced/katalog (TERPISAH dari `learning_sessions` M04 Session/live): `courses`+`course_lessons`, `learning_paths`+`learning_path_versions`, `learning_activities`+`learning_activity_completions`+`learning_unlock_progressions`, `enrollments` (Course Enrollment, distinct dari Session Enrollment), `quizzes`+`quiz_questions`+`quiz_options`+`quiz_attempts`, `certificates`. 7 permission baru (`m04.course.manage`, `m04.learning_path.manage`, `m04.learning_activity.manage`, `m04.learning_activity_completion.create`, `m04.course_enrollment.create`/`.view`, `m04.certificate.manage`) — master matrix 50-baris TIDAK PUNYA satu baris pun untuk Course/Learning Catalog. `0062` menutup FK `learning_sessions.course_id` yang ditunda sejak 0021. Diuji nyata lewat PostgREST menyeluruh (draft/publish visibility, quiz answer-key hidden dari Agent, self-issue certificate diblokir, no-client-mutation untuk unlock progression) — **tidak ada bug ditemukan**, desain tervalidasi sejak percobaan pertama. **REST API (Next.js + Zod), 26 route file**: `apps/web/app/api/courses/**`, `course-lessons/**`, `enrollments/**`, `agents/me/enrollments`, `quizzes/**`, `quiz-questions/**`, `agents/me/certificates`, `admin/certificates`, `learning/paths/**`, `learning/activities/**`, `agents/me/learning/**`, `admin/learning/activities/**` — dua route ADD-NEW keamanan-kritis di luar path literal STEP11-B4: `quizzes/{id}/take` (GET, admin client + strip manual `is_correct` sebelum dikirim ke Agent) dan `quizzes/{id}/submit` (POST, grading server-side exact-match per soal, dikunci ke `enrollment_id` milik sendiri). Diuji end-to-end nyata dengan 4 role (Superadmin/Instructor/Agent×2) lewat dev server — **tidak ada bug ditemukan**, termasuk verifikasi jawaban kunci quiz tidak pernah bocor dan hijack enrollment lintas-agent diblokir |
| **Fase 3 "100% tabel"** — M15 Awarding Engine | ✅ **Kode nyata + REST API (evidence-scoped)** | `supabase/migrations/0064`–`0070` — 9 tabel "mesin konfigurasi jalur/aturan kelulusan" yang ditunda sejak 0026: `awarding_paths`+`awarding_path_versions`, `awarding_rule_versions`+`awarding_path_rules` (junction N:N), `awarding_condition_groups`+`awarding_conditions`+`awarding_prerequisites`, `award_qualifying_paths` (provenance), `title_presentations`. Dengan ini **seluruh 14 tabel M15** (STEP11-B8 §13) lengkap. 2 permission baru (`m15.awarding_path_rule.configure` Superadmin/Admin/Manager saja, `m15.title_presentation.manage` Agent=own). `0069` menutup FK `qualification_evaluations`/`award_instances` → `awarding_path_versions`/`awarding_rule_versions` yang ditunda sejak 0026. Diuji nyata lewat PostgREST membangun satu rantai config engine utuh ujung-ke-ujung — tidak ada bug ditemukan. **REST API (Next.js + Zod), 9 route file** atas 9 endpoint API-205/207/208/209/210/212/213/214/215/234/235/236 yang eksplisit dievidence STEP11-B8: `titles/{id}/awarding-paths`, `awarding-paths/{id}` (+`/status`, `/versions`), `awarding-path-versions/{id}`, `awarding-rule-versions` (+`{id}`), `agents/me/awards/presentation`, `agents/{id}/awards/presentation` — plus enrichment route `awards/{id}/provenance` (API-229) yang sudah ada sekarang menyertakan `qualifying_paths`. **SENGAJA TIDAK dibangun**: endpoint untuk `awarding_path_rules`/`awarding_condition_groups`/`awarding_conditions`/`awarding_prerequisites` dan PATCH status untuk `awarding_path_versions`/`awarding_rule_versions` — STEP11-B8 F11-B8-002 s.d. F11-B8-006 eksplisit menyatakan "Do not invent" untuk keenamnya; 4 tabel tsb tetap migration+RLS-only (dikelola staff internal lewat Supabase langsung, bukan REST). Diuji end-to-end dengan 4 user throwaway (Superadmin/Manager/Agent×2) — **tidak ada bug ditemukan**, termasuk verifikasi isolasi RLS config-engine (Agent 403/404 di semua endpoint staff-only) dan hijack title-presentation lintas-agent diblokir (UNIQUE(user_id, title_definition_id) mencegah baris silang) |
| **Fase 4 "100% tabel"** — M14 Commercial (Midtrans sebagai payment gateway MVP) | ✅ **Kode nyata + REST API + integrasi Midtrans Sandbox sungguhan** | `supabase/migrations/0071`–`0078` — 8 tabel terakhir: `promotions` (staff-only), `addons` (katalog publik), `subscriptions` (privat), `commercial_orders`, `payment_transactions` (`payment_state` DIKUNCI ke vocabulary status Midtrans resmi), `payment_provider_results` (webhook mentah, TIDAK ADA jalur INSERT untuk siapa pun termasuk staf), `commercial_fulfillments` (juga tanpa jalur INSERT klien), `reconciliation_cases`. `0077` menutup 3 FK `commercial_entitlements.source_*` yang ditunda sejak 0019. **`0079` (batch REST API ini)**: fungsi `cancel_commercial_order()`/`fulfill_commercial_order()`/`allocate_quota_capacity()`/`consume_quota_capacity()` (SECURITY DEFINER, dijanjikan eksplisit di komentar 0072/0075), `grant_learning_points_from_purchase()` diperbarui merealisasikan TODO eksplisit di komentar 0025 (invocation M14→M04 kini lewat fulfillment_key sah, bukan cuma `is_superadmin()`), plus 2 trigger proaktif `enforce_commercial_order_insert_pending()`/`enforce_payment_transaction_insert_pending()` — ditemukan SEBELUM ada bug nyata bahwa RLS INSERT 0072/0073 tidak membatasi NILAI `status`/`payment_state` yang boleh diisi klien, ditutup sebelum REST API dibangun. **REST API (Next.js + Zod), 25 route file** — seluruh API-175–199 evidenced STEP11-B7 (§7): `commercial/catalog`/`products/{id}`/`offers` (baca publik = `addons`), `commercial/orders` (+`/checkout`/`/cancel`), `agents/me/commercial/orders`, `commercial/payments` (+`/{id}`), `integrations/payments/providers/midtrans/webhook`, `commercial/entitlements`(+`/{id}`)/`agents/me/entitlements`/`organizations/{id}/entitlements`, `admin/commercial/entitlements/reconcile`, `agents/me/quota`/`organizations/{id}/quota`/`commercial/quota/{id}`(+`/allocate`/`/consume`/`/usage`), `admin/commercial/reconciliation`(+`/{id}`/`/resolve`). **Integrasi Midtrans SUNGGUHAN diuji end-to-end** terhadap Sandbox asli (`app.sandbox.midtrans.com`, kredensial user sendiri) — bukan simulasi: POST checkout memanggil Snap API asli dan menerima `snap_token`/`redirect_url` sungguhan, webhook diverifikasi pakai signature SHA512 asli (Server Key), replay/duplicate webhook diabaikan idempoten, amount-mismatch dan status tak dikenal otomatis membuka `reconciliation_cases`. **2 bug ditemukan & diperbaiki saat testing** (di kode route batch ini sendiri, bukan migration): idempotency-key webhook awalnya hanya `order_id:status` — notification amount-salah bisa "mengunci" key sehingga notification BERIKUTNYA dengan amount benar ditolak sebagai duplicate (diperbaiki: key menyertakan `gross_amount`); error permission dari RPC `allocate`/`consume` awalnya jatuh ke 500 generik, bukan 403 (ditambahkan error-message mapping). **Keputusan keamanan terpenting**: `commercial_orders`/`payment_transactions` sengaja TIDAK PUNYA UPDATE untuk pemilik sama sekali DAN kini juga tidak bisa di-INSERT dengan status non-pending (trigger 0079) — dikonfirmasi lewat test bahwa Agent mencoba self-approve `payment_state`/`status` langsung lewat PostgREST mentah gagal total. **SENGAJA TIDAK dibangun** (evidence-scoped): CRUD admin katalog `promotions`/`addons` (F11-B7-002, dikelola staff langsung lewat Supabase), fulfillment subscription (tidak ada tabel katalog harga subscription plan yang dievidence, hanya addon-sourced order yang difulfill otomatis). **`0081` (ADD-NEW, ditemukan lewat testing nyata)**: `fulfill_commercial_order()` versi 0079 cuma memproses SATU pasang `capacity_type`/`capacity_value` per addon — addon "10 listing + 50 kuota refresh" yang dites sebelumnya membuktikan angka kedua di `configuration` TIDAK PERNAH benar-benar ter-grant. Ditambahkan kolom generik `addons.additional_capacities JSONB` (array kapasitas tambahan) + fungsi baru `grant_addon_capacity()` (satu unit grant, dipanggil berulang) — `fulfill_commercial_order()` (CREATE OR REPLACE, signature sama) kini loop kapasitas primer + seluruh `additional_capacities`. Diuji ulang nyata addon BARU "50 refresh + 25 listing tambahan" lewat Midtrans Sandbox sungguhan: terverifikasi 2 baris `commercial_entitlements`/`quota_capacities`/`operational_quota_pools`/`quota_allocations` dari satu fulfillment, keduanya kuota nyata (bukan metadata). Dengan Fase 1-4 selesai, **seluruh 97 tabel target sudah ada di Supabase**, dan modul demi modul REST API kini mencakup M04 Catalog, M15 Awarding, M14 Commercial |
| `packages/ui`, `packages/config` | ⏳ Belum ada kode | Placeholder |

> Modul-modul yang belum dikerjakan **masih murni dokumentasi/wireframe** —
> jangan asumsikan seluruh repo sudah bisa jalan penuh. **Seluruh 31 residual
> unik di `P9_CONTROLLED_ENGINEERING_RESIDUAL_REGISTER_v1.1.csv` sudah tertutup
> di level migration/RLS/fungsi (Tahap 1-6)**, ditambah 5 modul (M02/M05/M06
> lengkap/M08/M11) yang diminta eksplisit di luar checklist asli.
> Route REST (STEP-11) baru 15 slice (M10, M03 Listing + M14 Refresh, M09
> Admin Console, M08 Notifications, M05 Event, M06 Developer/Project/Media/
> Marketing Kit/Claim, M13 Provider/BYOK, M04 Learning Session/Evidence, M04
> LP Economy + Partnership Learning Result, M15 Qualification/Evidence/
> Awarding, M04 Learning Catalog/Activity (Fase 2), M15 Awarding Engine
> (Fase 3), M14 Commercial (Fase 4), dan Fase 1 "100% tabel" + M02 Profile —
> semua lengkap atas skema yang ada, kecuali beberapa tabel config-engine
> M15/admin-katalog M14/M12 `organization_document`/verification-document
> management yang dokumen sumbernya sendiri eksplisit melarang endpoint-nya,
> lihat baris Fase 1/3/4 dan M02). M14 Commercial mencakup integrasi
> Midtrans Snap API + webhook SUNGGUHAN (Sandbox), bukan simulasi. **Dengan
> Fase 1-4 "100% tabel" selesai, seluruh 94 entitas logis STEP10-D + 3 tabel
> ADD-NEW di luar STEP10-D (api_idempotency_keys, notification_templates,
> partnership_learning_results) = 97 tabel total sudah ada di Supabase, DAN
> seluruh modul REST API — termasuk M04 Catalog, M15 Awarding, M14
> Commercial, Fase 1 (M01/M03/M07/M11/M12), dan M02 Profile — kini semuanya
> punya lapisan REST API di atas migration+RLS-nya (kecuali controlled gap
> yang dievidence eksplisit sebagai "jangan dibangun").**

## Struktur folder

```
RumahAgen-SaaS/
├── README.md                      # file ini
├── PROJECT_STRUCTURE.md           # urutan baca untuk implementation agent
├── CHECKLIST_RESIDUAL_IMPLEMENTASI.md   # 31 residual unik, dipetakan ke modul + urutan kerja
├── docs/
│   ├── core/current/              # STEP 00–14 + P-SERIES (governance, arsitektur, data, API, RBAC, PRD, dst.)
│   │   └── source-pack-index/
│   └── design/
│       └── wireframes/            # WF-00 s.d. WF-11
├── archive/
│   ├── source-packs-current/      # zip asli STEP/P-series (provenance)
│   └── wireframe-source-packs/    # 27 zip asli WF (provenance)
├── audit/
│   ├── FULL_RECURSIVE_CORPUS_MANIFEST.csv
│   ├── CURRENT_SOURCE_PACK_MANIFEST.csv
│   ├── DEEP_SCAN_REPORT.md
│   ├── WIREFRAME_DEEP_SCAN_REPORT.md
│   └── M10_PERMISSION_SEED_TRACEABILITY.csv   # lacak balik 90 permission_code ke baris asal di master matrix
├── apps/
│   └── web/                       # Next.js — scaffold + middleware API global + M10 route contoh
├── packages/
│   ├── ui/                        # KOSONG
│   └── config/                    # KOSONG
├── supabase/
│   ├── migrations/                # 0001–0097: M10 + M09 + M13 + M03/M14 + M04/M15 + M02/M05/M06/M08/M11 + 0038 index performa + 0039/0040/0042 fix gap RLS DELETE + 0041 fix RLS review-klaim + 0043-0045 fix RLS/trigger attendance-completion + 0046 fungsi adjust_learning_points + 0047-0053 Fase 1 "100% tabel" (13 tabel M01/M03/M07/M11/M12 + index) + 0054-0055 fix RLS status-check & self-approval + 0056-0063 Fase 2 "100% tabel" (13 tabel M04 Learning Catalog + FK fix + index) + 0064-0070 Fase 3 "100% tabel" (9 tabel M15 Awarding Engine + FK fix + index) + 0071-0078 Fase 4 "100% tabel" (8 tabel M14 Commercial, Midtrans MVP + FK fix + index) + 0079 fungsi REST API M14 (cancel/fulfill order, allocate/consume quota, fix TODO grant_learning_points_from_purchase, trigger anti self-approval INSERT) + 0080 M13 kolom kredensial generik (public_identifier + encrypted_secondary_key untuk provider multi-kredensial mis. Cloudinary) + 0081 M14 kuota majemuk per addon (addons.additional_capacities + grant_addon_capacity() + fulfill_commercial_order() diperluas) + 0082 fix keamanan has_permission() menegakkan users.status suspended/rejected (ditemukan lewat audit pra-Bolt.new) + 0083 keputusan produk: hapus gate Pending Review, users.status default 'active' langsung pasca-OTP (dikonfirmasi sesuai audit wireframe WF-01) + 0084 fix 3 bug permission M09 (audit log Manager/Admin tertukar, Provider Catalogue Admin hilang, Reconciliation Manual Correction seharusnya Superadmin-only) + 0085 fix status "withdrawn" hilang di agent_project_claims + 0086 listing status suspended (enforcement) + 0087 organization status closing/suspended + auto-cancel join-request + 0088 event registration approval mode (auto_confirm/manual_approval/closed) + 0089 M07 Bank Master + threshold per-bank + share/revoke DBR (gap terbesar dari deep scan Core) + 0090/0091 fix RLS & SECURITY DEFINER yang ketemu saat testing nyata 0086/0088/0089 + 0092 agent_profiles Provinsi/Kota/Organization + 0093 M13 BYOK satu-koneksi-hidup-per-provider + state unverified + 0094 koreksi konflik STEP12-01 vs 0084 (Audit Log & Provider Catalogue dikembalikan sesuai Master Matrix) + 0095 rate limiting Postgres rate_limit_log (ADR-018, ganti in-memory Map) + 0096 trigger sinkron auth.users↔public.users + 10 route M01 Identity/Auth REST API (register/verify-otp/resend-otp/login/oauth-google/refresh/logout/logout-all/forgot-password/reset-password — Gap #1 dari deep scan .docx/.zip, Google OAuth+OTP kode diverifikasi login/email asli) + 0097 M11 SEO/Discovery: tabel seo_config + 7 route (sitemap-index/listings/agents/developer-projects.xml, robots.txt, admin/config/seo, admin/seo/reindex — Gap #2 dari deep scan .docx/.zip) + M13 POST /ai-assistant/chat (Gap #3, tanpa migration baru — adapter OpenAI/Anthropic/Gemini di lib/ai/adapters.ts, diverifikasi panggilan Gemini asli sungguhan) — audit Core vs migrasi (Tier 1-3 + 65 file .docx/.zip STEP-09/11/12) SELESAI, SELURUH 97 tabel target selesai (lihat migrations/README.md)
│   └── functions/                 # KOSONG
└── .github/workflows/             # KOSONG
```

## Urutan baca (untuk developer atau AI coding agent)

1. `docs/core/current/00-governance/` — aturan, gate, silsilah rekonsiliasi.
2. `docs/core/current/02-architecture/` — Constitution, System Architecture, Technical Decisions.
3. `docs/core/current/03-data/` — ERD, Dictionary, Schema (Step 10).
4. `docs/core/current/04-api/STEP-11-API-SYNCHRONIZATION` — kontrak API.
5. `docs/core/current/05-authorization/STEP-12-RBAC-PERMISSION-RLS-SYNCHRONIZATION` — sumber `supabase/migrations/0001`–`0010`, `0015`–`0016`.
6. `docs/core/current/00-governance/STEP-00/PRE-00-E_M03_LISTING_REFRESH_GATE_FULL_v1.1.md` — gate khusus, sumber aturan bisnis `supabase/migrations/0018`, `0020`.
7. `docs/design/wireframes/` — referensi visual per modul.
8. **`CHECKLIST_RESIDUAL_IMPLEMENTASI.md`** — mulai dari sini untuk tahu apa yang perlu dikerjakan berikutnya dan urutannya.
9. `supabase/migrations/README.md` dan `apps/web/README.md` — detail teknis apa yang sudah jalan.

## Menjalankan yang sudah ada

```bash
# 1. Push migration M10 + M09 + M13 + M03/M14 + M04/M15 + M02/M05/M06/M08/M11 ke project Supabase Anda
supabase link --project-ref <project-ref>
supabase db push

# 2. Jalankan scaffold Next.js
cd apps/web
cp .env.example .env.local   # isi dari Supabase Dashboard
npm install
npm run dev
```

## Batasan implementasi runtime

Repo ini **sebagian sudah punya kode**, sebagian masih spesifikasi. Jangan
mengasumsikan seluruh SaaS sudah bisa dijalankan — hanya M10 Authorization, M09
Admin Console, M03 Listing + M14 Refresh Allowance, M08 Notifications, M05
Event, M06 Developer/Project/Media/Marketing Kit/Claim, M13 Provider/BYOK, M04
Learning Session/Evidence, M04 LP Economy + Partnership Learning Result, M15
Qualification/Evidence/Awarding + Awarding Engine (lapisan REST-nya evidence-
scoped — 4 dari 14 tabel M15, yakni `awarding_path_rules`/
`awarding_condition_groups`/`awarding_conditions`/`awarding_prerequisites`,
SENGAJA tanpa endpoint karena STEP11-B8 eksplisit melarangnya, tetap
migration+RLS-only), M04 Learning Catalog/Activity, M14 Commercial
(lapisan REST-nya evidence-scoped juga — admin CRUD `promotions`/`addons`
SENGAJA tanpa endpoint karena STEP11-B7 F11-B7-002 eksplisit melarangnya,
fulfillment otomatis hanya untuk addon-sourced order, subscription purchase
belum ada katalog harganya), Fase 1 "100% tabel" (M01 verification-document
intake-only, M03 listing media/price-history/leads/amenities, `ref_villages`,
M12 organization invitations, M11 `url_redirects`, M07 `dbr_simulations`),
dan M02 Profile (`agent_profiles`/`agent_reviews`, `organization_document`
M12 SENGAJA tanpa endpoint karena STEP11-B6 eksplisit melarangnya) —
semuanya lapisan database + REST API, termasuk integrasi Midtrans Snap API
+ webhook sungguhan (Sandbox) — dan fondasi API yang sudah ada
implementasinya. Modul bisnis lain (dst.)
menyusul sesuai urutan di `CHECKLIST_RESIDUAL_IMPLEMENTASI.md`.
