// Tailwind v4 dipasang lewat plugin PostCSS (tanpa tailwind.config.js: token ada di app/globals.css bagian @theme).
const config = {
  plugins: {
    "@tailwindcss/postcss": {},
  },
};

export default config;
