# STEP09-F Package Hash Policy v1.2 RERUN-01

The final ZIP SHA256 is recorded in an external SHA256 file to avoid circular self-hash claims. Internal evidence manifests hash input artifacts and output artifacts other than the final ZIP itself.
